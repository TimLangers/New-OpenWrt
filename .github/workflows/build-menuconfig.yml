name: OpenWrt-合集版

on:
  workflow_dispatch:
  schedule:
    - cron: '0 16 * * *'   # 北京时间次日 00:00

env:
  REPO_URL: https://github.com/openwrt/openwrt.git
  REPO_BRANCH: openwrt-25.12
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: OK2.config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: 检查项目分支
      uses: actions/checkout@main

    - name: 清理磁盘空间
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: true
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        swap-storage: true

    - name: 查看机器配置
      id: machine_info
      run: |
        echo "警告⚠"
        echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
        echo -e "CPU性能排序: 7763 > 8370C > 8272CL > 8171M > E5-2673\n"
        echo "--------------------------CPU信息--------------------------"
        CPU_PHYSICAL=$(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)
        CPU_CORES=$(nproc)
        CPU_MODEL=$(cat /proc/cpuinfo | grep -m1 name | awk -F: '{print $2}' | sed 's/^[ \t]*//')
        echo "CPU物理数量: $CPU_PHYSICAL"
        echo "CPU核心数量: $CPU_CORES"
        echo -e "CPU型号信息:$CPU_MODEL\n"
        echo "--------------------------内存信息--------------------------"
        MEM_INFO=$(free -h | awk '/^Mem:/ {print $2}')
        echo "内存总大小: $MEM_INFO"
        echo "--------------------------硬盘信息--------------------------"
        DISK_COUNT=$(ls /dev/sd* | grep -v [1-9] | wc -l)
        DISK_USAGE=$(df -hT)
        echo "硬盘数量: $DISK_COUNT" && echo "$DISK_USAGE"
        echo "CPU_PHYSICAL=$CPU_PHYSICAL" >> $GITHUB_ENV
        echo "CPU_CORES=$CPU_CORES" >> $GITHUB_ENV
        echo "CPU_MODEL=$CPU_MODEL" >> $GITHUB_ENV
        echo "MEM_INFO=$MEM_INFO" >> $GITHUB_ENV

    - name: 安装编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev jq
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: 发送编译开始通知
      run: |
        read -r -d '' MSG << EOF || true
        🚀 *OpenWrt 编译开始* 🚀
        📅 *时间*：$(date '+%Y-%m-%d %H:%M:%S')
        📦 *仓库名称*：${{ github.repository }}
        🖥️ *编译服务器信息*：
          • CPU：${{ env.CPU_MODEL }}
          • 物理数量：${{ env.CPU_PHYSICAL }}
          • 核心数：${{ env.CPU_CORES }}
          • 内存：${{ env.MEM_INFO }}
          • 磁盘使用：$(df -h / | awk 'NR==2 {print $4}') 可用
        📦 *配置文件*：${{ env.CONFIG_FILE }}
        🔧 *源码分支*：${{ env.REPO_BRANCH }}
        EOF
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -H "Content-Type: application/json" \
          -d "{\"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\", \"text\": \"$MSG\", \"parse_mode\": \"Markdown\"}"

    - name: 克隆固件源码
      working-directory: /workdir
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: 复制 feeds 配置
      run: |
        # 从 New-OpenWrt 子目录复制 feeds 配置文件（如果存在）
        if [ -e "New-OpenWrt/$FEEDS_CONF" ]; then
            cp "New-OpenWrt/$FEEDS_CONF" openwrt/feeds.conf.default
        else
            echo "警告: New-OpenWrt/$FEEDS_CONF 不存在，跳过复制"
        fi

    - name: 更新 & 安装 feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 加载自定义配置与 DIY 脚本
      run: |
        cd openwrt
        
        # 复制自定义配置文件（如果存在）
        [ -e $GITHUB_WORKSPACE/$CONFIG_FILE ] && cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
        
        # 强制确保 x86_64 架构配置（防止因配置文件不完整导致架构错误）
        echo "CONFIG_TARGET_x86_64=y" >> .config
        echo "CONFIG_TARGET_x86_64_DEVICE_generic=y" >> .config
        
        # 生成/刷新配置
        make defconfig
        
        # 立即校验架构
        if ! grep -q "CONFIG_TARGET_x86_64=y" .config; then
            echo "❌ 架构配置失败，无法继续"
            exit 1
        fi
        echo "✓ 架构确认：x86_64"
        
        # 执行 DIY 脚本（此时 .config 已包含正确架构，不会触发脚本内的 exit 1）
        chmod +x $GITHUB_WORKSPACE/$DIY_P1_SH $GITHUB_WORKSPACE/$DIY_P2_SH
        $GITHUB_WORKSPACE/$DIY_P1_SH
        $GITHUB_WORKSPACE/$DIY_P2_SH
        
        # 最终刷新配置
        make defconfig
        
        # 最终校验
        if [ ! -f .config ]; then 
          echo "❌ .config 未生成"; exit 1
        fi
        grep -q "CONFIG_TARGET_x86_64=y" .config || { echo "❌ 最终架构校验失败"; exit 1; }
        
        echo "✅ 配置加载与 DIY 脚本执行完成"

    - name: 下载编译依赖包
      run: |
        cd openwrt
        make download -j8
        find dl -size -1024c -exec rm -f {} \;

    - name: 编译固件
      id: compile
      run: |
        cd openwrt
        echo "FILE_DATE=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
        make -j$(nproc) || make -j1 V=s

    - name: 发送编译结果通知 (Telegram)
      if: always()
      run: |
        if [ "${{ job.status }}" = "success" ]; then
          MSG="✅ *OpenWrt 编译成功* ✅\n[下载地址](https://github.com/${{ github.repository }}/releases/tag/OpenWrt_${{ env.FILE_DATE }})"
        else
          MSG="❌ *OpenWrt 编译失败* ❌\n查看日志: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        fi
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -H "Content-Type: application/json" \
          -d "{\"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\", \"text\": \"$MSG\", \"parse_mode\": \"Markdown\"}"

    - name: 上传固件到 Releases
      uses: softprops/action-gh-release@v1
      if: success() && env.UPLOAD_RELEASE == 'true'
      with:
        tag_name: OpenWrt_${{ env.FILE_DATE }}
        name: OpenWrt 自动编译 ${{ env.FILE_DATE }}
        files: openwrt/bin/targets/**/*.img*
